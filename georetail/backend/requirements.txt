# ─── Web framework ────────────────────────────────────────────────────────────
fastapi==0.135.1
uvicorn[standard]==0.41.0
python-multipart==0.0.9       # necesario para subida de archivos

# ─── Base de datos ─────────────────────────────────────────────────────────────
asyncpg==0.29.0               # driver (conector) PostgreSQL asíncrono
redis[asyncio]==5.0.8         # cliente Redis asíncrono

# ─── Cola de tareas (procesamiento en segundo plano) ──────────────────────────
celery==5.4.0
kombu==5.3.4                  # capa de mensajería para Celery

# ─── Scheduler (tareas programadas) ───────────────────────────────────────────
apscheduler==3.10.4

# ─── LLMs (modelos de lenguaje) ───────────────────────────────────────────────
anthropic==0.84.0             # Claude (Anthropic)
openai==2.26.0                # GPT-4o / DeepSeek / Kimi (compatible OpenAI)
google-genai==1.56.0          # Gemini Flash (Google — nuevo SDK unificado)
langchain==1.2.12
langchain-anthropic==1.3.4

# ─── Machine Learning ─────────────────────────────────────────────────────────
xgboost==2.1.1
scikit-learn==1.5.2
shap==0.46.0                  # explica qué variables influyen en el score
optuna==3.6.1                 # búsqueda de hiperparámetros (configuración óptima del modelo)
numpy==1.26.4
pandas==2.2.3
duckdb==1.1.3                 # auditoria local de CSV/ZIP demograficos
cleanlab==2.7.0               # deteccion de filas sospechosas para datasets de ML
joblib==1.4.2                 # guardar/cargar modelos en disco

# ─── NLP (procesamiento de lenguaje natural) ──────────────────────────────────
# NOTA: Python 3.14 no tiene wheels para torch<2.5 ni transformers<5. Pinneamos
# a versiones que sí resuelven en 3.14 (verificado 2026-04-20 con pip install
# sentence-transformers torch --upgrade).
sentence-transformers==5.4.1  # convierte textos en vectores numéricos
torch==2.11.0                 # PyTorch (base de sentence-transformers; primer major con wheels py3.14)
transformers==5.5.4

# ─── Geoespacial (mapas y coordenadas) ────────────────────────────────────────
shapely==2.0.6                # manipulación de polígonos en Python
pyproj==3.6.1                 # conversión entre sistemas de coordenadas
mercantile>=1.2.1             # tile math (XYZ ↔ lat/lng), usado por vcity pipeline
mapbox-vector-tile>=2.1.0     # decodifica MVT (Mapbox Vector Tiles) desde VCity BSC tileserver

# ─── HTTP (llamadas a APIs externas) ──────────────────────────────────────────
httpx[http2]==0.28.1          # cliente HTTP asíncrono (http2 para scrapers)
aiohttp==3.10.5

# ─── Scraping anti-bot ────────────────────────────────────────────────────────
curl-cffi==0.7.4              # impersona TLS fingerprint de Chrome/Firefox (derrota la mayoría de anti-bots)
playwright==1.49.0            # browser automation para páginas JS-heavy — portales inmobiliarios (instalar: playwright install chromium)
lxml==5.3.0                   # parser HTML rápido para extracción de datos
# gosom scraper: run via Docker (gosom/google-maps-scraper)
#   docker run -p 8080:8080 -e API_KEY=$GOSOM_API_KEY gosom/google-maps-scraper
#   httpx (cliente REST) ya incluido arriba en sección HTTP

# ─── Generación de PDFs ───────────────────────────────────────────────────────
weasyprint==62.3              # genera PDFs desde HTML/CSS
jinja2==3.1.4                 # plantillas HTML para los PDFs

# ─── Validación y configuración ───────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2      # leer variables de entorno (.env)
python-dotenv==1.0.1

# ─── Utilidades ───────────────────────────────────────────────────────────────
tenacity==9.0.0               # reintentos automáticos en llamadas a APIs
python-dateutil==2.9.0
pytz==2024.2
